#include <fstream>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

vector<vector<int>> Transition;
vector<vector<int>> Advance;

vector<vector<int>> getTransitionTable() { return {{}}; }

vector<vector<int>> getAdvanceTable(vector<vector<int>> &Transition) {
  return {{}};
}

int main() { return 0; }